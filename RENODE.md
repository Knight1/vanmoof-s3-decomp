# Renode

```
wget https://builds.renode.io/renode-latest.deb
sudo apt-get install policykit-1 libgtk2.0-0 screen uml-utilities gtk-sharp3 libc6-dev libicu-dev 
gcc python3 python3-pip
sudo dpkg -i renode-latest.deb
pip install -r /opt/renode/tests/requirements.txt
```